---
max_turns: 8
allowed_tools: [Read, Glob, Grep, Skill]
---

I'm building an app in the Forge over MCP. To test it I want the agent to be
able to seed twenty fake orders and wipe them again — but that must never be
possible once the app is installed. How would you set that up and what happens
to it when we ship?
